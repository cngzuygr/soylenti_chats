import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SplashScreen = () => {
	return (
		<View>
			<Text style={{ alignSelf: "center" }}>Yükleniyor</Text>
		</View>
	);
};

export default SplashScreen;

const styles = StyleSheet.create({});
